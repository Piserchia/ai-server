# atlas-value-research GOTCHAS

- Universe breadth card must track the UNMEASURABLE rate — adding 500
  names that all fail F-computability is not breadth.
- EDGAR cache lives in the workspace clone (cold each run): budget minutes
  for fetches at ≤10 req/s; never lift the throttle.
- Never weaken the earnings veto or obligation-based sizing — owner-owned
  doctrine; propose via DECISION-REQUEST only.
