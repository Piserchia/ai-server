# CLAUDE.md — <SLUG>

You are working in the `<SLUG>` project. It is a React static site (Vite build)
hosted at `<SLUG>.chrispiserchia.com` via Caddy file_server serving the `dist/` directory.

After making changes, run `npm run build` to regenerate `dist/`.

Read `.context/CONTEXT.md` before making changes. Write back to
`.context/CHANGELOG.md` after.
