# CLAUDE.md — <SLUG>

You are working in the `<SLUG>` project. It is a FastAPI service hosted at
`<SLUG>.chrispiserchia.com` via Caddy reverse_proxy on port <PORT>.

Read `.context/CONTEXT.md` before making changes. Write back to
`.context/CHANGELOG.md` after.
