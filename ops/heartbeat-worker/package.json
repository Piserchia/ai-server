{
  "name": "ai-server-heartbeat",
  "version": "1.0.0",
  "private": true,
  "description": "External dead-man's-switch that polls ai-server /health and alerts Telegram on outage",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.60.0"
  }
}
